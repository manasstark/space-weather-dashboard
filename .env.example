# Copy this file to .env and fill in real values. .env is gitignored.

# NASA DONKI API key (used for CME fetches in src/swdss/features/build_master.py).
# Get a free key at https://api.nasa.gov/ — without it, requests fall back to
# NASA's shared "DEMO_KEY", which is rate-limited.
NASA_API_KEY=
